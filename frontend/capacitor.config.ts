import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wanderlust.nest',
  appName: 'Wanderlust Nest',
  webDir: 'www/browser',
};

export default config;
